# GenerateConfig for EVMbench paper reproduction (optional)
#
# Provenance: paper does not specify temperature or max_tokens;
# agents run until the 6-hour timeout with provider defaults.
#
# Usage:
#   inspect eval evmbench/evmbench_detect \
#       --task-config src/evmbench/paper_config/evmbench.yaml \
#       --generate-config src/evmbench/paper_config/generate.yaml \
#       --model openai/gpt-5.1-2025-11-13

max_tokens: 4096
