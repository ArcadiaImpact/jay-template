# STUB: Docker Compose configuration for the sandbox environment.
# The agent runs commands inside this container.
services:
  default:
    build:
      context: .
      dockerfile: Dockerfile
    # STUB: Add resource limits as needed
    mem_limit: 2g
    cpus: 2
    init: true
